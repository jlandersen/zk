#!/bin/bash

if [[ -z "${ZK_PATH}" ]]; then
    echo "ZK_PATH must be set"
    exit 1
fi

if [[ -z "${ZETTLE_HOME}" ]]; then
    echo "ZETTLE_HOME must be set"
    exit 1
fi

tmux new-session -A -s "zk" -c "${ZETTLE_HOME}" "bash -l -c -i vim" \; split-window -h "zk_fzf_view"\;
